# Improvement Backlog

Last updated: 2026-03-20

## Verified Current State

- `npm ci` passes without `.npmrc`
- `npm run lint` passes
- `npm run test:ci` passes
- `npm run build -- --configuration=production --verbose` passes and writes `dist/tomatenstaude`
- `npx playwright install chromium` passes
- `npm run e2e` passes
- `npm run a11y:test` passes
- `npm run a11y:ci` passes

## Remaining Work

None required for the current cleanup branch.
