import { PrologAckermannComponent } from './prolog-ackermann.component';

describe('PrologAckermannComponent', () => {
  it('should create', () => {
    const component = new PrologAckermannComponent();
    expect(component).toBeTruthy();
  });
});
