import { PerlIndexComponent } from './perl-index.component';

describe('PerlIndexComponent', () => {
  it('should create', () => {
    const component = new PerlIndexComponent();
    expect(component).toBeTruthy();
  });
});
